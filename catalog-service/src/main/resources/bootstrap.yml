server:
  port: 0
spring:
  application:
    name: catalog-service
#  profiles:
#    active: dev
---
spring:
  profiles: default
  cloud:
    config:
      uri: http://localhost:8888
spring.profiles.active: dev