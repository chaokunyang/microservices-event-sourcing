spring:
  profiles:
    active: development
---
spring:
  profiles: development
  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: update
    # disable open EntityManager in View
    open-in-view: false
  datasource:
    url: jdbc:mysql://localhost:3306/online_store?useUnicode=true&characterEncoding=utf-8
    username: online_store
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
security:
  oauth2:
    resource:
      userInfoUri: http://localhost:8181/user
    client:
      client-id: elasticjee
      access-token-uri: http://localhost:8787
eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/